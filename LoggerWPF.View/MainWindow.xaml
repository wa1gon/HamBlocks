<Window x:Class="LoggerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:LoggerWPF.Core;assembly=LoggerWPF.Core"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <core:MainViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="80" />
        </Grid.RowDefinitions>
        
        <Menu FontSize="20" Grid.Row="0" Margin="5">
            <MenuItem Header="_File">
                <MenuItem Header="_Setting" Command="{Binding SettingsMenuClickCommand}" />
            </MenuItem>
            <MenuItem Header="_Awards" />
            <MenuItem Header="_WebLogs" />
        </Menu>
        
        <StackPanel Grid.Row="1" Margin="10">

        </StackPanel>
        
        <StackPanel Grid.Row="2" Background="Aquamarine">
            
        </StackPanel>
    </Grid>
</Window>
