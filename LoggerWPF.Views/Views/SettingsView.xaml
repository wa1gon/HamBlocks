<UserControl x:Class="LoggerWPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Background="Yellow">
            <TextBlock Text="{Binding Message}" FontSize="20" Margin="0,0,0,10" />
            <DataGrid ItemsSource="{Binding Configs}" AutoGenerateColumns="False" Height="200">
                <!-- <DataGrid.Columns> -->
                <!--     <DataGridTextColumn Header="Name" Binding="{Binding Name}" /> -->
                <!--     ~1~ <DataGridComboBoxColumn Header="Option"  @1@ -->
                <!--     ~1~                         ItemsSource="{Binding DataContext.Options, RelativeSource={RelativeSource AncestorType=UserControl}}" @1@ -->
                <!--     ~1~                         SelectedItemBinding="{Binding SelectedOption}" /> @1@ -->
                <!-- </DataGrid.Columns> -->
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
