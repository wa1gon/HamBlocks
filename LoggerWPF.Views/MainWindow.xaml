<Window x:Class="LoggerWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:Logger.Core;assembly=Logger.Core"
        xmlns:views="clr-namespace:LoggerWPF.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="800">
    <Window.DataContext>
        <core:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type core:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type core:HomeViewModel}">
            <views:HomeView />
        </DataTemplate>
    </Window.Resources>
    <!-- <Window.Background> -->
    <!--     <ImageBrush ImageSource="/Assets/HamBlocksBG.png" -->
    <!--                 Stretch="UniformToFill" /> -->
    <!-- </Window.Background> -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*"  />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <!-- <views:SettingsView Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/> -->
        <Menu FontSize="15" Grid.Row="0" Margin="1" Background="LightGray">
            <MenuItem Header="_Home" Command="{Binding HomeMenuClickCommand}" />
            <MenuItem Header="_File">
                <MenuItem Header="_Setting" Command="{Binding SettingsMenuClickCommand}" />
            </MenuItem>
            <MenuItem Header="_Awards" Command="{Binding AwardsMenuClickCommand}" />
            <MenuItem Header="_WebLogs" Command="{Binding WebLogsMenuClickCommand}" />
        </Menu>
        
        <!-- <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="10" /> -->
        <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" />
        
        <StackPanel Grid.Row="2" Background="LightGray">
            <!-- Optional content for footer -->
        </StackPanel>
    </Grid>
</Window>           
